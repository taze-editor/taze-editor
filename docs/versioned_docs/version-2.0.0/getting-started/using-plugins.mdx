---
slug: /using-plugins
title: Using Plugins
---

Let's use basic marks plugins. You can use `mod+key` to toggle marks. For
instance: `mod+b` to toggle bold, `mod+i` to toggle italic, `mod+u` to toggle
underline.

import { UsingPlugins } from "./UsingPlugins";

<UsingPlugins />
